name: CI - Main
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
jobs:
  Build:
    uses:   azure-prep/roboshop-github-actions/.github/workflows/ci-dev.yml@main
    with:
      appType: 'Java'
      appName: 'shipping'
    secrets:
      vault_token: ${{ secrets.TOKEN }}